[book]
authors = ["Umesh Kumar Singh"]
language = "en"
src = "src"
title = "Linux Shell Scripting and Automation"

[build]
build-dir = "book"                # the directory where the output is placed
create-missing = true             # whether or not to create missing pages
use-default-preprocessors = true  # use the default preprocessors
extra-watch-dirs = []             # directories to watch for triggering builds

[output.html.fold]
enable = true    # whether or not to enable section folding
level = 0         # the depth to start folding

[output.html]
default-theme = "rust"
preferred-dark-theme = "coal"
smart-punctuation = true
mathjax-support = true
no-section-label = false
git-repository-url = "https://github.com/qbit-glitch/shellclass"
site-url = "https://qbit-glitch.github.io/shellclass/"
input-404 = "not-found.md"
editable = true         # allows editing the source code
copyable = true          # include the copy button for copying code snippets
line-numbers = true     # displays line numbers for editable code


[output.html.print]
enable = true    # include support for printable output
page-break = true # insert page-break after each chapter
